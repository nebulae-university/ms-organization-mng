apiVersion: v1
kind: Service
metadata:
  name: orga-mng-mongo
spec:
  ports:
  - port: 27017
    name: orga-mng-mongo
  selector:
    app: orga-mng-mongo
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orga-mng-mongo-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.5Gi
---
apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: orga-mng-mongo
spec:
  selector:
    matchLabels:
      app: orga-mng-mongo
  strategy:
    type: Recreate      
  template:
    metadata:
      labels:
        app: orga-mng-mongo
    spec:
      containers:
      - image: mongo:4.2
        name: orga-mng-mongo        
        ports:
        - containerPort: 27017
          name: orga-mng-mongo
        volumeMounts:
        - name: orga-mng-mongo-persistent-storage
          mountPath: /data/db
        resources:
          requests:
            cpu: "50m"
      volumes:
      - name: orga-mng-mongo-persistent-storage
        persistentVolumeClaim:
          claimName: orga-mng-mongo-pv-claim      